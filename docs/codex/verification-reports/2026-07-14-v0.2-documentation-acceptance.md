# Verification Report：v0.2 文档一致性验收

## 元信息

- Verification ID: `VERIFY-20260714-v0.2-documentation-acceptance`
- Date: 2026-07-14
- Subject Commit: `a5d48d170f0cca23652595d9babdd98139775d49`
- Branch: `codex/hukou-v0.2-docs-acceptance`
- PR: `https://github.com/rtwsvj/hukou/pull/5`
- Environment: macOS 27.0 Darwin 27.0.0 arm64
- Go: `go1.26.5 darwin/arm64`
- Verdict: **pass**
- GitHub-hosted Runner Gate: **infrastructure-blocked**；Run `29336431803` 的四个 jobs 均为 0 steps
- Related Change: `CHANGE-20260714-v0.2-documentation-acceptance`

本轮只修改项目文档与 Codex 记录，没有改动 Go 源码、依赖、workflow、tag 或 Release。三路独立只读复核分别检查文档体系、H2 实现/测试和 Git/GitHub 发布对象；最终未发现 P0/P1、假完成或阻断 V0.2 的缺口。

## Claims vs Evidence

| Claim | 核验方式 | 结果 |
|---|---|---|
| 当前事实文档不再把 H2 写成“验证中” | 对照 `docs/README.md`、roadmap、H2/Release verification reports | pass |
| 项目简报与阶段规格包含 doctor 和已交付 WAL 边界 | 对照 CLI、transaction/doctor 实现、requirements、architecture、ADR-0003 | pass |
| 历史 ADR 未被改写，当前边界不夸大 | ADR-0001/0002 保持不变；brief/spec 明确硬件掉电、外部 writer TOCTOU 与 repair 后置 | pass |
| 发布报告的 tag/main 关系在 closeout merge 后仍准确 | `git merge-base --is-ancestor 60554bf... origin/main` exit 0 | pass |
| 文档记录链完整 | execution report → change record → 本 verification report → session log | pass |
| V0.2 实现没有假完成 | 独立审查 WAL/doctor 接线与定向测试；普通/race 各 401 tests | pass |
| GitHub 发布对象真实有效 | tag object/peel、PR #3/#4、Release ID 353518203、五个 asset size/digest 与 checksums | pass |

## 本轮运行

| 命令/检查 | Exit | 关键结果 |
|---|---:|---|
| Markdown relative-link checker | 0 | 52 个 Markdown 文件，无缺失相对目标 |
| `make verify COVERAGE=/tmp/hukou-v0.2-docs.out BINARY=/tmp/hukou-v0.2-docs` | 0 | mod verify、vet、test、race、coverage、build 全绿 |
| `go tool cover -func=/tmp/hukou-v0.2-docs.out` | 0 | total statements 73.8% |
| 独立 `go test ./...` | 0 | 401 passed / 16 packages |
| 独立 `go test -race ./...` | 0 | 401 passed / 16 packages |
| `git diff --check` | 0 | 无输出 |
| `git merge-base --is-ancestor 60554bf... origin/main` | 0 | release commit 仍在当前 main 历史 |
| GitHub PR/Release/tag/asset API 复核 | 0 | PR #3/#4 merged；Latest Release 非 draft/prerelease；5 assets 一致 |
| PR #5 CI Run `29336431803` | 远端 failure | 4 jobs / 0 steps；annotation 明确 billing/spending-limit，infrastructure-blocked |

## 修正的漂移

1. `docs/README.md` 的 H2 状态仍停留在“最终验证进行中”。
2. `docs/00-project-brief.md` 漏列 doctor，并把进程级 `SIGKILL` 恢复整体写成未实现。
3. `docs/specs/phase2-adopt-upgrade.md` 仍写“H1 修订中”，命令/数据布局漏 doctor、backup 与 transaction。
4. 发布报告把发布时的 `main` tip 写成会永久等于当前 `origin/main`。
5. 文档地图未直接索引历史来源与许可证边界。

## 未重复运行与剩余边界

- 本轮是文档一致性验收，没有重复启动容器、重新生成四平台 archive 或重新下载全部 Release 资产；这些字节级证据仍由 `2026-07-14-v0.2.0-release.md` 约束。本轮重新核对了当前 Git/GitHub 元数据、asset size/digest、checksums 与一个 Linux archive 的结构。
- GitHub-hosted runner 仍因账户 payment/spending limit 无法分配，不能声称远端 CI 通过或代码失败。
- Release 仍为 `immutable=false`；tag 未签名，也没有 provenance/attestation。
- 自动 repair、激活历史/可配置 retention、公共 fixture smoke、Windows 仍在 roadmap，不阻断已定义的 V0.2。
