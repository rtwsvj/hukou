# Verification Report：V0.3 Private Release Candidate（进行中）

## 元信息

- Verification ID: `VERIFY-20260714-v0.3-private-rc`
- Date: 2026-07-14
- Subject Commit: pending
- Branch: `codex/hukou-v0.3-private-rc`
- PR: pending
- Environment: pending final capture
- Go: pending final capture
- Related Change: `CHANGE-20260714-v0.3-private-rc`
- Verdict: **inconclusive / in progress**
- Repository Visibility: private；未改变
- Release Status: no v0.3 tag or Release
- GitHub-hosted Runner Gate: pending；已知账户 billing/spending limit 风险，不得预写为 pass

本文件是验证记录骨架，不是通过证明。只有绑定最终 subject commit、完成下列门禁并
由 pinhaoma-review 对照 change record 后，才能把 verdict 改为 pass/partial/fail。

## Claims vs Evidence

| Claim | 当前证据 | 当前结论 |
|---|---|---|
| C1 trust-first 零写/网络边界 | 工作树定向测试含目录快照/network spy；最终 fixed-commit 证据待汇总 | partial |
| C2 ownership boundary 与共享 checker | 安全关键路径 audit 与全仓 ordinary/race 阶段通过 | partial |
| C3 schema v2/history/rollback | schema-specific required fields、legacy smuggling、safe tag/tag-SHA tests 已进入 321/6 audit；全仓 ordinary/race 各 641/21 | partial |
| C4 policy/pagination/pin | Go strict sortable SemVer 与 shell release SemVer 各有合法/非法矩阵；最终 commit 复跑 pending | partial |
| C5 history-aware retention | original 完整性、store topology、activation binding 定向回归已通过；最终 fault matrix 待固化 | partial |
| C6 repair fingerprint/preconditions | stale 保证明确为零业务状态写；最终 fixed-commit 明细待汇总 | partial |
| C7 support privacy | secret/输出模式回归进入阶段全仓；最终 fixed-commit 明细待汇总 | partial |
| C8 installer/license/supply chain | release-verify、installer link(2)/rename(2)、duplicate-member、workflow static pass；snapshot/SBOM/remote pending | partial |
| C9 English/docs consistency | 68 Markdown/89 relative targets 无缺失；production 汉字 sweep 0；最终 review pending | partial |
| C10 fixed-commit RC/readiness/draft private PR | subject commit/PR/release snapshot 不存在 | pending |

## 已完成的局部运行

| 命令/批次 | Exit | 关键结果 |
|---|---:|---|
| 安全关键路径定向 audit | 0 | 321 passed / 6 packages |
| `go test -count=1 ./...` | 0 | 最新 direct uncached：641 passed / 21 packages；最终 commit 仍需复跑 |
| `go test -count=1 -race ./...` | 0 | 最新 direct uncached：641 passed / 21 packages；最终 commit 仍需复跑 |
| `GOPROXY=https://goproxy.cn,direct make release-verify` | 0 | 全 target 通过；coverage total 72.9%；govuln `No vulnerabilities found` |
| 默认 `make vulncheck` | non-zero | `proxy.golang.org` IPv6 timeout；命令级 mirror override 已成功，默认路径不记 pass |
| `scripts/install_test.sh` | 0 | link(2)/rename(2)、duplicate member、dangling/symlink race pass |
| `scripts/release_test.sh` | 0 | strict shell SemVer matrix pass；不证明 snapshot |
| actionlint 1.7.12 / Ruby YAML parse | 0 | workflows 静态 pass |
| official Action tag pin 对账 | 0 | 固定 SHA 与上游官方 tag 匹配 |
| Markdown relative links | 0 | 68 Markdown / 89 relative targets，0 missing |
| production Go/shell/workflow 汉字 sweep / `git diff --check` | 0 | 0 汉字命中；diff pass |
| Linux/arm64 non-root ordinary/race | 0 | all packages pass；UID 65532、repo read-only、GOPROXY mirror |
| GNU tar 1.34 installer/release tests | 0 | pass；release test 配置 git safe.directory 后通过 |

## 必须补齐的最终门禁

| Gate | 命令/证据 | 状态 |
|---|---|---|
| formatting/diff/module/vet | fixed commit 上重跑并记录 | stage pass；final commit pending |
| full ordinary/race | 最终 commit uncached `go test ./...`、`go test -race ./...`，记录 test/package count | stage ordinary/race pass；final commit pending |
| coverage/build | fixed commit isolated coverage path、buildinfo smoke | stage 72.9% / build pass；final commit pending |
| license/installer/shell/vuln | fixed commit 重跑；保留默认 proxy timeout 与 mirror override 两条事实 | stage pass with documented network-path exception |
| read-only/privacy/fault matrix | trust-first、history、retention、repair、support 定向/强杀/漂移测试 | pending |
| cross-platform | fixed commit 上 macOS + native Linux ordinary/race；darwin/linux × amd64/arm64 build | stage native Linux pass；final commit/build matrix pending |
| reproducible snapshot | 两次正式 release script、byte diff、4 checksums、archive contents/buildinfo | pending |
| SBOM/supply chain | SPDX JSON parse/content、workflow YAML、public/private visibility gates | pending |
| docs | Markdown relative links、CLI/code facts、Chinese production-string sweep | stage pass；final review pending |
| independent review | `$pinhaoma-review` 对 C1-C10 与 git diff/测试证据 | pending |
| Git/GitHub | subject commit、push、draft private PR、hosted job step/annotation inspection | pending |

## Gap audit 修复状态

最新直接 audit 复跑全仓 ordinary/race 各 641 tests / 21 packages，并未发现剩余
P0/P1；安全关键路径定向 audit 为 321 tests / 6 packages。其结论仍是
最终验收的输入，不替代固定 commit 的 verification report 或 pinhaoma-review。

- Installer non-force 已增加 dangling-symlink 预检；有 Perl时使用 `link(2)` atomic
  no-replace，force 使用 `rename(2)`，Linux 无 Perl时回退 `ln -T`/`mv -T`。
  directory、symlink-to-directory、预检后竞争和 duplicate target member tests 已
  通过；最终 subject commit 仍需复跑。
- Release workflow 已删除手动 snapshot 的历史 `v0.1.0` default；最终 YAML/dispatch
  contract 仍需复核。
- Strict `manifest.Decode` 已统一用于 command/doctor/repair；invalid v2 backup、unknown
  field、schema-specific required fields、schema 0/1 携带 v2-only 字段、孤立 checksum
  evidence 和 symlink rollback E2E 已补覆盖。
- Gap audit 后续两个 P2 已关闭：Store.Versions 对非目录/畸形 version fail closed
  并有两组测试；explain 增加 name/path 独立目录快照和
  `http.DefaultTransport` spy，5 项定向通过。
- Activation entry points 拒绝 unsafe tag，并保持历史内同 tag 只能绑定同一 SHA；
  `list` 会先验证 original namespace 完整，再统计不含 original 的下载版本。
- Installer 最终目录项操作使用 `link(2)` atomic no-replace / force `rename(2)`；
  Linux 无 Perl回退 `ln -T`/`mv -T`（缺少安全 primitive 时失败关闭），并拒绝
  directory、symlink-to-directory、竞争目标和重复目标 member；shell release SemVer 与 Go update
  policy 的 strict boundary 分别有回归矩阵。

以下仍是后续 defense-in-depth，不属于本报告已完成项：duplicate JSON key rejection、
GitHub API response body cap、installer 总解压体积/member 数上限，以及基于
`openat`/目录 fd 的路径锚定。

## 当前限制与禁止声明

- 可以引用本工作树上的 641/21 ordinary/race 与 mirror override 下
  `make release-verify` 和 non-root Linux 阶段通过，但不得写成“最终 fixed commit
  已通过”。最终 fixed-commit Linux gate、release snapshot 与 SBOM 仍不得声称通过。
- 不得声称 GitHub-hosted CI 绿色；0-step billing/spending failure 只能标 infrastructure-blocked。
- CodeQL/attestation 在 private repository 中按条件跳过，不等于通过。
- 不得创建/声称 `v0.3.0` tag、Release、公开仓库、Homebrew Tap 或公共 fixture repo。
- 局部测试若在后续代码变化前运行，必须在最终 subject commit 重新运行或明确降级证据等级。

## 最终 closeout 模板

完成后补充：commit/PR/environment/Go、精确命令与 exit、test count/coverage、artifact
checksums/SBOM、remote job URLs/steps、pinhaoma-review findings 与修复、剩余风险，以及
最终 verdict。若任一必须门禁未完成，verdict 只能是 partial/fail/inconclusive。
