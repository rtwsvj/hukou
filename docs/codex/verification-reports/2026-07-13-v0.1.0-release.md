# Verification Report：v0.1.0 发布验证

## 元信息

- Verification ID: `VERIFY-20260713-v0.1.0-release`
- Date: 2026-07-13
- Version: `v0.1.0`
- Subject Commit: `d15331dbe4d258d54253643b758c787bb63c95e1`
- Tag: annotated tag `v0.1.0`，目标为上述 commit
- Release: <https://github.com/rtwsvj/hukou/releases/tag/v0.1.0>
- Build Environment: 两个相互独立的全新 `golang:1.26.5-bookworm` 容器
- Native Smoke Environment: macOS Darwin arm64
- Release Verdict: **pass**
- GitHub-hosted Runner Gate Verdict: **infrastructure-blocked**

Release 本身通过：同一正式脚本在两个全新容器中的独立构建逐字节一致，四个归档的 checksum、双平台 buildinfo、远端五个资产均已复核。GitHub-hosted runner 门禁没有通过也没有发生代码或测试失败；三个最终 Actions run 均因账户 payment/spending limit 在 runner 调度前停止，所有实际 runner steps 为空。

## Claims vs Evidence

| Claim | 核验方式 | 结果 |
|---|---|---|
| 正式 tag 精确指向发布 commit | GitHub tag API 显示 `v0.1.0` 为 annotated tag，tag object 指向 `d15331dbe4d258d54253643b758c787bb63c95e1` | pass |
| Release 已发布且不是预发布 | GitHub Release metadata：`isDraft=false`、`isPrerelease=false` | pass |
| Release 资产集合完整 | 远端共 5 个资产：Darwin/Linux × amd64/arm64 四个归档与 `checksums.txt` | pass |
| 正式脚本可重复生成发布产物 | 两个全新的 `golang:1.26.5-bookworm` 容器分别运行仓库正式 `scripts/release.sh`；两组输出逐文件、逐字节比较 | pass |
| checksum 覆盖且验证全部归档 | `checksums.txt` 含四个精确文件名；4/4 归档校验成功 | pass |
| 版本元数据绑定发布 commit | Linux arm64 与 Darwin arm64 归档解包后运行 `hukou version`，均得到 `v0.1.0`、完整 commit 和 `2026-07-13T16:58:19Z` | pass |
| GitHub 上的资产就是已验证的本地产物 | 从 Release 全新下载全部 5 个资产，与本地已验证产物逐字节比较 | pass |
| GitHub-hosted Linux/macOS runner 完成发布门禁 | 三个 run 在 runner 分配前因账户计费/额度限制停止；没有实际 step 结果 | infrastructure-blocked |

## 发布资产

| Asset | Size | SHA-256 |
|---|---:|---|
| `hukou_0.1.0_darwin_amd64.tar.gz` | 3,972,793 bytes | `fa1f6079a15684c70f6f286810daae328d9cc4337f3c460ad5d029c86fa20eb5` |
| `hukou_0.1.0_darwin_arm64.tar.gz` | 3,645,799 bytes | `4d27a66318450b34c000ec899385ab366b445a6ba89718e5d3658d73b3e8193b` |
| `hukou_0.1.0_linux_amd64.tar.gz` | 3,910,273 bytes | `7cbd6601c7bdf8e36a82bf0f8fe878e787a56fe02403a25ce2bab274a57921c9` |
| `hukou_0.1.0_linux_arm64.tar.gz` | 3,507,978 bytes | `21c991f2c4389586f26b184ab4293f7656cf8e8cb3da1b61923a2fb614554e5b` |
| `checksums.txt` | 390 bytes | `b1d7d6c9e748e92b1deed66d46bff37aae05a220413384b2d3e7ab5e510aa50e` |

四个 archive 的 SHA-256 同时满足三处一致：正式脚本生成的 `checksums.txt`、本地独立复核结果、GitHub Release asset digest。远端重新下载后还对全部五个文件做了逐字节比较，结果一致。

## 可重复构建与运行验证

1. 临时启动原本处于停止状态的 Colima；没有复用已有构建容器。
2. 在两个全新 `golang:1.26.5-bookworm` 容器中，从同一 commit 分别运行正式 `scripts/release.sh`，版本参数为 `v0.1.0`。
3. 对两次输出的四个归档和 `checksums.txt` 做逐文件比较；全部逐字节相同。
4. 用 `checksums.txt` 校验每组四个归档；两组均为 4/4 通过。
5. 解包 Linux arm64 和 Darwin arm64 归档并运行二进制；两者的版本输出均精确为：
   - Version: `v0.1.0`
   - Commit: `d15331dbe4d258d54253643b758c787bb63c95e1`
   - Build date: `2026-07-13T16:58:19Z`
6. 发布后从 GitHub 重新下载四个归档与 `checksums.txt`；全部与本地已验证文件逐字节一致。
7. 验证完成后停止 Colima，恢复其任务开始前的停止状态。

上述 fallback 直接使用仓库正式发布脚本，没有另写或放宽一套临时打包逻辑；它证明确定 commit 的发布脚本、四平台产物、checksum、buildinfo 与实际远端资产一致。

## GitHub Actions 基础设施例外

| Run | Event / Subject | 观察结果 | 判定 |
|---|---|---|---|
| [`29268485688`](https://github.com/rtwsvj/hukou/actions/runs/29268485688) | PR CI，head `5092f29dd194dc468806ab37e395c47a8fbc7bfe` | quality、coverage、Ubuntu、macOS 四个 job 均未启动，`steps=[]` | infrastructure-blocked |
| [`29268603489`](https://github.com/rtwsvj/hukou/actions/runs/29268603489) | manual release snapshot，head `d15331dbe4d258d54253643b758c787bb63c95e1` | Linux/macOS release gate 均未启动，`steps=[]`；package/publish 随后 skipped | infrastructure-blocked |
| [`29268975174`](https://github.com/rtwsvj/hukou/actions/runs/29268975174) | `v0.1.0` tag push，head `d15331dbe4d258d54253643b758c787bb63c95e1` | Linux/macOS release gate 均未启动，`steps=[]`；package/publish 随后 skipped | infrastructure-blocked |

三个 run 的 GitHub annotation 均说明 job 未启动，原因是近期账户付款失败或 spending limit 需要提高。这是 runner 调度前的账户基础设施拒绝，不是 checkout、build、test、race、coverage、package 或 publish step 的执行失败。

因此本报告明确不声称以下事项：

- 不声称这三个最终 Actions run 通过。
- 不声称最终 commit 已在 GitHub-hosted Linux 与 macOS runner 上完成远端 CI。
- 不把 workflow 页面显示的 `failure` 解释为代码或测试失败。

## 验证边界与遗留门禁

- Release verdict `pass` 证明已发布的五个远端资产与两次隔离正式构建的已验证产物完全一致。
- 本次容器 fallback 覆盖正式 Linux/GNU tar 打包环境和 Linux arm64 运行 smoke；Darwin arm64 二进制另在原生 macOS 上运行验证。
- fallback 不能代替 GitHub-hosted runner 环境本身的 Linux/macOS CI 证据。账户计费/额度恢复后，仍应补跑 PR/main CI 与手动 release snapshot，并把实际 runner 结果另行记录。
- 已发布 `v0.1.0` tag 和资产保持不可变；补跑 runner gate 或后续修复不得移动 tag、覆盖资产，代码修复使用新的 patch release。

## 最终结论

`v0.1.0` 的 tag、已发布 Release、四平台 archive、checksums、确定性重建、双平台 buildinfo smoke 和远端资产一致性均已验证，故 **Release pass**。

GitHub-hosted Linux/macOS runner 从未获得执行机会，故对应门禁为 **infrastructure-blocked**，不是 pass，也不是代码/测试 fail。该例外已完整保留，未用本地或容器结果伪装成远端 CI 结果。
