# Codex 执行报告：v0.2 文档一致性验收

## 元信息

- Execution ID: `EXEC-20260714-v0.2-documentation-acceptance`
- Date: 2026-07-14
- Authorization: 用户要求建立完善文档系统并持续推进到 V0.2，且已明确授权自主推进与推送 GitHub
- Branch: `codex/hukou-v0.2-docs-acceptance`
- Base: `main@728fc22b07f0e9d65276d1ac71a7add81af33161`
- Status: approved and in progress

## 目标

对已经发布的 `v0.2.0` 做一次只读事实复核，修正文档入口、项目简报、阶段规格和发布报告中与当前状态不一致或容易误读的内容，补齐本次执行、改动与验证记录。

## 计划变更

1. 把文档总入口的 H2 状态从“验证中”同步为已发布、已验证，并补历史来源索引。
2. 在项目简报与 Phase 2 规格中补入 doctor、WAL 与当前 crash-safety 边界。
3. 把发布报告的 `main` 表述改为带时点的祖先关系，避免 closeout 合并后产生假冲突。
4. 运行文档链接、`git diff --check`、`make verify`，并独立核对实现、Git/GitHub tag、PR、Release 与资产。
5. 写入 change record、verification report、session log，使用独立分支和 PR 合并。

## 不变范围

- 不改 Go 代码、manifest schema、CLI 行为或 release workflow。
- 不移动或重签 `v0.2.0` tag，不覆盖已发布资产。
- 不把 GitHub-hosted runner 的 billing/spending-limit 阻塞写成代码通过或失败。
- 不宣称自动 repair、公共 fixture smoke、Windows、签名或 attestation 已完成。

## 风险与回滚

- 主要风险是把历史 ADR 的时点边界误改成当前事实；历史 ADR 保持不变，只修当前事实文档。
- 文档变更集中在独立分支，可通过单独 revert 回滚；发布对象不受影响。

## 验收条件

- 当前事实文档彼此一致，所有相对 Markdown 链接有效。
- `make verify` 与 `git diff --check` 通过。
- tag/PR/Release/资产元数据与验证报告一致。
- change record、verification report 与 session log 形成可追溯闭环。
