# Codex 改动记录：V0.3 Private Release Candidate

## 元信息

- Change ID: `CHANGE-20260714-v0.3-private-rc`
- Date: 2026-07-14
- Branch: `codex/hukou-v0.3-private-rc`
- Base: `main@bd4faa32d9b5b604b1b224f97fe891ed670f3742`
- Subject Commit: pending
- Related Execution: `../execution-reports/2026-07-14-v0.3-private-rc.md`
- Status: implementation complete；fixed-commit verification/commit pending
- Verification Status: partial targeted evidence；full RC gate pending

## 用户请求

在保持仓库 private、不提前发布 `v0.3.0` 的前提下推进 V0.3 private RC，并持续
维护可由 `$pinhaoma-review` 对照代码、diff、测试和发布证据核验的文档记录。

## 声称完成的改动清单

以下 checkbox 表示“已被最终 review 证明”，不是“工作树中看到了文件”。本记录
创建时全部保留未勾选，最终 verification/review 后逐项更新。

- [ ] C1：trust-first 入口形成 explain → adopt dry-run/outdated → real mutation 的闭环。
  - Evidence expected: CLI help、stable JSON、零写/零网络或 metadata-only 测试。
  - Files: `cmd/explain.go`、`cmd/adopt.go`、`cmd/outdated.go`、`cmd/inventory.go`、`internal/output/`、`internal/updatecheck/`。
- [ ] C2：`upgrade --all` 只处理 hukou entries，且 outdated/dry-run/real upgrade 共享 policy-aware checker。
  - Evidence expected: ownership-boundary tests、request/download counters、Topgrade 文档。
  - Files: `cmd/upgrade.go`、`cmd/outdated.go`、`internal/updatecheck/`、`docs/integrations/topgrade.md`。
- [ ] C3：manifest v2 deterministic migration、schema-specific strict validation、activation lineage 与 parent-based rollback 完整接线。
  - Evidence expected: v0/v1→v2、legacy v2-only field smuggling、future/unknown/malformed rejection、safe tag 与 tag/SHA binding、A→B→C→B→A 与 WAL after-state tests。
  - Files: `internal/manifest/`、`internal/activation/`、`cmd/adopt.go`、`cmd/upgrade.go`、`cmd/rollback.go`。
- [ ] C4：SemVer/GitHub-latest、channel、exact pin、rollback depth policy 由 CLI 与 update checker 一致执行。
  - Evidence expected: policy unit/race tests、GitHub list/by-tag tests、downgrade/pin/channel matrix。
  - Files: `cmd/policy.go`、`internal/versionpolicy/`、`internal/updatecheck/`、`internal/ghrelease/`。
- [ ] C5：retention 不读取 mtime，完整保护 current/original/pin/lineage；pending 或 malformed state 零删除。
  - Evidence expected: deterministic PlanPrune/ApplyPrunePlan、stale plan、malformed original、pending journal tests。
  - Files: `internal/store/store.go`、`internal/store/prune_test.go`、`cmd/upgrade.go`。
- [ ] C6：repair 只开放两个 fingerprint-bound action；stale/ambiguous/unsafe state 不修改业务状态。
  - Evidence expected: plan read-only snapshot、0600、fingerprint mismatch、transaction/backup/live precondition tests。
  - Files: `cmd/repair.go`、`internal/repair/`。
- [ ] C7：support bundle 离线、默认脱敏、不上传，stdout 零写、file 为 0600。
  - Evidence expected: secret fixture negative assertions、directory snapshot、output mode tests。
  - Files: `cmd/support.go`、`internal/supportbundle/`。
- [ ] C8：安装、许可证、community、CI/release supply-chain 准备已落盘且不会误公开/误发布。
  - Evidence expected: license/install/shell/vuln gates、link(2)/rename(2) 与重复 archive member 测试、YAML parse、archive contents、SBOM、visibility conditions。
  - Files: `scripts/`、`LICENSE`、`LICENSES/`、`THIRD_PARTY_NOTICES.md`、community files、`.github/`、`Makefile`。
- [ ] C9：英文默认 CLI、双语 README 与 project docs 准确区分 implemented / RC verified / publicly released。
  - Evidence expected: Chinese production-string sweep、Markdown links、CLI help、documentation claim audit。
  - Files: `README.md`、`README.zh-CN.md`、`CHANGELOG.md`、`docs/`。
- [ ] C10：最终 private RC 在固定 commit 完成全仓/跨平台/release snapshot/pinhaoma-review，并只推送 private branch/draft PR。
  - Evidence expected: final verification report、commit SHA、draft private PR、remote gate 状态；不存在 v0.3 tag/public visibility change。
  - Files: `docs/codex/verification-reports/2026-07-14-v0.3-private-rc.md` 与 Git/GitHub 对象。

## 当前实际修改范围

| 范围 | 动作 | 说明 |
|---|---|---|
| `cmd/` | modified/created | trust-first、policy、repair、support、history-aware upgrade/rollback 与英文 CLI |
| `internal/{activation,manifest,store,updatecheck,versionpolicy,repair,supportbundle,ghrelease,doctor,output,provenance,archive}/` | modified/created | schema/history/policy/retention/repair/privacy 与兼容修正 |
| `scripts/`、`Makefile` | modified/created | installer/demo/license/release gates 与打包内容 |
| `.github/` | modified/created | community health、CI、CodeQL、release SBOM/attestation 条件 |
| 根项目文件 | modified/created | Apache-2.0、notices、双语 README、CHANGELOG 与社区治理文件 |
| `docs/specs/`、`docs/adr/`、`docs/integrations/` | modified/created | V0.3 规格、长期决策与 Topgrade 所有权边界 |
| `docs/00-*` 至 `docs/10-*`、`docs/codex/` | modified/created | 当前事实、风险、发布与执行证据链同步 |

最终提交前必须用 `git diff --name-status <base>...HEAD` 替换/补充为精确文件清单。

## 已运行命令与当前结果

| 命令/批次 | 结果 | 说明 |
|---|---|---|
| 安全关键路径定向 audit | pass：321 tests / 6 packages | manifest schema、activation、archive/store 等最新修复的工作树阶段证据 |
| `go test -count=1 ./...` | pass：641 tests / 21 packages | 最新 direct uncached ordinary；最终 commit 仍需复跑 |
| `go test -count=1 -race ./...` | pass：641 tests / 21 packages | 最新 direct uncached race；最终 commit 仍需复跑 |
| `GOPROXY=https://goproxy.cn,direct make release-verify` | exit 0 | fmt/mod/vet/test/race/coverage/build/license/install/release-test/shellcheck/govuln；coverage 72.9% |
| pinned `govulncheck@v1.5.0`（上述 mirror override 内） | pass | `No vulnerabilities found`；默认 proxy.golang.org 路径另有 IPv6 timeout |
| `scripts/install_test.sh` | pass | 含 link(2)/rename(2) 最终提交、dangling/symlink race、重复 archive member |
| `scripts/release_test.sh` | pass | strict shell SemVer 合法/非法矩阵；不证明 release snapshot |
| actionlint 1.7.12 / Ruby YAML parse / official Action tag pins | pass | workflow 静态与固定 SHA 对账；不证明 hosted run |
| Markdown relative links | pass：68 Markdown / 89 targets，0 missing | 当前工作树链接检查 |
| production Go/shell/workflow 汉字 sweep / `git diff --check` | pass | 0 汉字命中；diff 无 whitespace error |
| Linux/arm64 non-root ordinary/race + GNU tar installer/release tests | pass | UID 65532、只读 repo、mirror；最终 commit 仍需复跑 |

## 验证结果

- 已验证（工作树阶段）：安全关键路径定向 321/6；全仓 direct uncached
  ordinary/race 各 641/21；mirror override 下 `make release-verify` exit 0、coverage
  72.9%、govuln 无已知漏洞；installer/release-test/actionlint/YAML/链接/语言/diff gates。
- 已在实现和测试中新增：schema-specific required fields 与 legacy v2-only smuggling
  rejection、activation safe tag 与 tag/SHA binding、installer link(2)/rename(2) 目录项
  语义与重复 member 拒绝、shell/Go 各自严格 SemVer 边界、`list` original 完整性检查。
- 未验证：最终固定 commit 的全仓/Linux 复跑、最终四目标/双构建、
  release snapshot/SBOM、remote Actions、最终 commit/draft PR。
- 默认 `make vulncheck` 的 `proxy.golang.org` IPv6 路径 timeout；命令级镜像覆盖成功
  不能改写为默认网络路径通过。
- 需要 `$pinhaoma-review` 回顾：是。
- 当前不能勾选 C1-C10，也不能把本记录解释为 V0.3 已发布或仓库已公开。

## 未完成事项

- 在固定 commit 复跑全部门禁并固化环境、命令和输出证据。
- 生成/检查 private release snapshot、checksums、archive contents 与 SPDX SBOM。
- 在最终 commit 复跑 installer link(2)/rename(2)、duplicate-member、dangling-symlink/
  no-force 竞争测试与 release manual snapshot YAML 检查。
- 运行 pinhaoma-review，关闭所有 P0/P1/假完成。
- 提交、推送 private branch、创建 draft private PR；记录 hosted Actions 的真实状态。
- 后续 defense-in-depth：拒绝重复 JSON key、限制 GitHub API body、限制安装器总解压
  体积/member 数，并评估 `openat`/目录 fd 锚定；这些不是当前已关闭 claims。

## 回滚

本批次位于独立分支。未合并前可关闭 draft private PR/删除分支而不改变 v0.2.0 tag 与
Release；禁止移动已发布 tag。Manifest v2 写入真实用户状态后不能用 v0.2 静默降级，
必须依赖迁移前备份或在 V0.3 内继续操作。
