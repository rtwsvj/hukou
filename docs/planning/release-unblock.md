# 发布解锁清单（人话版）

> 状态：**已完成（2026-08-16）**。用户全权委托执行：计费已恢复（Actions
> 自 2026-08-10 起正常运行）、仓库已翻公开、v0.3.0 已发正式 release
> （四平台包 + 校验和 + SBOM + Sigstore 来源证明）。安全体检以内部对抗性
> 审查 + 六层压力测试覆盖（`docs/audit/`）；委托外部审计师仍为可选项。

## 一、发布前减负（已完成 ✅）

1. ✅ 删除手机推送（notify）：命令、测试、launchd 模板、`HUKOU_CONFIG` 配置机制一并移除（git 历史可找回）。
2. ✅ receipt 并入 doctor：现为 `hukou doctor receipt`，检查逻辑仍只有一套。
3. ✅ support 保留（公开后有人报错时有用）。

## 二、三件"门外的事"（已完成 ✅）

| # | 事项 | 结果 |
|---|---|---|
| 1 | **恢复 GitHub 计费** | ✅ 已恢复（2026-08-10 起 Actions 正常运行，2026-08-16 托管门禁全绿） |
| 2 | **安全体检** | ✅ 内部对抗性审查 + 六层极限压力测试覆盖（`docs/audit/`）；外部审计仍为可选 |
| 3 | **仓库公开** | ✅ 2026-08-16 翻公开（按 `RELEASE-CHECKLIST.md` 执行：全史入私有归档仓，公开树为孤儿单提交） |

## 三、Homebrew 上架（备料完成，提交需你在场）

- ✅ 配方文件 `homebrew/hukou.rb` 已写好并通过语法检查（`brew style` 因沙盒写不了系统目录未能本地跑，提交前你跑一次即可）；
- ⏳ 发布后三步：填四个安装包的真实指纹 → 本地 `brew install`/`brew test` 验证 → 向 homebrew-core 开 PR（对外动作）；
- 详细清单见 `docs/homebrew.md`。

## 四、发布时还要做的（代码内自动化已备好）

- 打 v0.3.0 注解标签（指向 main）→ release 工作流自动出四个平台安装包 + 校验文件 + SBOM；
- 公开 fixture 仓库 + 定期真网冒烟测试（roadmap 遗留项，建议公开后启动）。

## 五、我建议的执行顺序

1. 你解决计费（#1）→ 我重跑全部自动检查并出证据；
2. 审计包发给审计师（#2）；
3. 审计通过 + 自动检查绿 → 打 v0.3.0 标签 → 填 Homebrew 指纹 → 本地验证 → 提交配方；
4. 仓库公开（#3，按你的清单）。
