# 下一步计划（人话版）

> 状态：2026-08 已按你的决定排序（C → B → A）。你看过这份文档后动工。
> 你的三个决定记录在文末，将来改主意随时说。

## 总路线：先中文，再功能，最后发布

你定的顺序：

1. **C 队：中文界面** —— 先做（详细方案见《中文界面方案》）。
2. **B 队：六个实用功能** —— 中文做完之后做。
3. **A 队：正式对外发布** —— 放最后。

## C 队：中文界面（先做）

把软件在终端里显示的所有文字变成中文（默认仍英文，想用中文开一个开关）。

范围（你已拍板）：帮助文字、表格、报告、报错信息——**①到③一步到位全做**；程序来源说明文字暂缓。（原计划的"手机通知摘要"已随手机推送功能的取消一并删去，见 A 队减负。）

具体做法、分几步、有什么坑，全写在《中文界面方案》里，这里不重复。

## B 队：六个实用功能（做完中文后做）

按推荐顺序：

### B1. 搬家清单（抄 gup、stew 的作业）✅ 已完成（2026-08）

- 现在：程序上了户口后，账本只存在这台电脑里。
- 已加：`hukou export` 把上了户口的程序导出成可迁移清单（JSON，含仓库/标签/指纹/策略）；到新电脑上 `hukou import` 按单重新登记（程序须已在 PATH 上，完整重跑收编安检，绝不下载）。像搬家公司给的打包清单。
- 为什么有用：换电脑、帮同事配电脑时，不用一个个手点。
- 实测：中英双模式全流程（导出→清空→重新登记→账本一致）通过；dry-run 零写入；坏清单（版本不符/多余字段/重名/坏仓库）全部拒绝。

### B2. 帮忙猜来源（思路来自 eget、bget）✅ 已完成（2026-08）

- 现在：给程序上户口时要手填"它从哪个网站来的"；填不出就只能登记为"无来源"。
- 已加：`hukou suggest <name|path>` 自动去 GitHub 搜候选仓库（精确名 → 名称包含 → 描述命中 → 星标排序），并给出可直接复制运行的收编命令（含最新标签、预发布/归档标注）。**只建议、不擅自动手**，你不同意就不写；找不到精确匹配时明说"仅显示相关候选"。
- 为什么有用：上户口最烦的一步就是填来源，这一步省掉，上户口体验就顺了。
- 实测：真实网络验证（suggest bat → sharkdp/bat 排第一并给出 v0.26.1）；严格只读（连数据目录都不创建）；中英双模式；坏网络/限额/无候选全部干净报错。

### B3. 升级前先看说明（抄 topgrade、ubi 的思路）✅ 已完成（2026-08）

- 现在：升级前只告诉你"有新版"，不告诉你改了啥。
- 已加：`upgrade --dry-run` 与真实升级都会显示发布说明摘要（最多 8 行×100 列）；跨主版本（1.x→2.x）时在 outdated/dry-run/真实升级三处都醒目标注"主版本跳跃（可能有破坏性变更）"；资产选择做到与 GitHub 返回顺序无关的确定性，Linux 上优先选 musl 版本（抄 ubi）。
- 为什么有用：少踩坑，升级更有底。
- 实测：真实 ripgrep 发布说明中文渲染正确；策略原因文案（"有更高的符合条件语义化版本可用"）也中文化；新增 5 组测试（主版本判定矩阵、musl 偏好、输入顺序无关、dry-run 展示说明+警告、更新检查警告注入）。

### B4. 一键升级改版（抄 topgrade）✅ 已完成（2026-08）

- 现在：一键升级的输出像流水账。
- 已加：真实运行打印逐步轨迹——每个管理器带 `==> 名称：命令` 标题，随后是 `成功/失败(退出码)/已取消` 结果行；PATH 上没有的显示"跳过"；新增 `--retry N` 让失败的外部管理器自动重试（进程内步骤不重试）；失败不中断其余、最终汇总非零退出；JSON 模式下轨迹走 stderr，stdout 保持纯 JSON。
- 为什么有用：升级出问题时，能马上知道卡在哪。
- 实测：中文实弹运行显示"==> hukou：进程内升级"与校验、升级成功行；新增 4 组测试（轨迹+跳过、重试成功、重试耗尽、JSON 纯净）。

### B5. 指纹防掉包（抄 aqua）✅ 已完成（2026-08）

- 现状如实盘点：升级前的活动文件指纹核对与收编时的原始备份本就存在；真正缺的是"原始备份自身的防篡改锚定"。
- 已加：账本新字段 `adopted_sha256`——收编那一刻的指纹，永远不被升级/回滚改写；`doctor` 把版本库里的原始备份与它比对，对不上就报"版本库可能被篡改"（错误级）；旧条目无锚定则跳过（不误报）；导出清单同步携带该字段。
- 为什么有用：防止程序在不知不觉中被换过（比如被恶意软件替换）。
- 实测：中文实弹——健康体检通过 → 篡改原始备份 → 体检立刻报"原始备份与收编锚定指纹不符（版本库可能被篡改）"；新增 4 组测试（字段校验、收编落锚、篡改检出、旧条目不误报）。

### B6. 上架官方渠道（Homebrew）✅ 备料完成（2026-08，正式提交需你批准）

- 现在：装 hukou 要用一条下载脚本。
- 已备好：配方 `homebrew/hukou.rb`（四平台安装包、安装/冒烟测试、许可证文件随包分发）通过语法检查；`docs/homebrew.md` 写明发布后三步（填指纹→本地验证→向 homebrew-core 开 PR）与提交前提。
- 为什么有用：降低安装门槛，也显得正规。
- 未做（如实）：沙盒无法运行 `brew style`（要写系统目录）、未真装（未经你同意不装真实机器）、未提交（对外动作）。

## A 队：正式对外发布（放最后）

现在的新版程序已经做完，但还没正式发布。A 队分两块：

### 第一块：发布前减负（你已拍板）

1. ✅ **删掉手机推送（notify）**（2026-08 已执行）：命令、测试、launchd 模板、`HUKOU_CONFIG` 配置机制一并移除（git 历史可找回）。
2. ✅ **receipt 并入 doctor**（2026-08 已执行）：现为 `hukou doctor receipt`，顶层 receipt 命令已删除，检查逻辑只留一套。
3. ✅ **support 保留**：现在没公开、用不上，但公开后有人来报错时有用——留着。
4. **修复"收编被拒留下垃圾"缺陷（极限测试发现的真 bug）**：给一个程序起"original"这种保留名收编时，程序会先写备份、后拒绝，结果留下半截空目录，还把账本搞成"体检不过"。修法：先查名字合不合法、再写任何东西（先验后写）。✅ **已修复（2026-08）：保留名在写任何东西之前就被拒绝，零残留；带回归测试。**
5. **支持"安装包里的程序名和本机名不一样"（极限测试发现的真缺口）**：现在收编时起的名字如果跟安装包里的程序名对不上，以后升级会一直失败（比如给 ripgrep 起个别的名字）。修法：收编时允许记录"安装包里真正叫啥"（抄 eget/ubi 的现成做法），升级时照这个找。✅ **已修复（2026-08）：`adopt --exe` + 账本字段 `archive_exe`；沙盒实弹验证"改名收编 ripgrep → 升级成功"。**

### 第二块：三件"门外的事"

1. **欠费**：GitHub 那边的自动检查因为账户计费问题一直没跑起来（要解决账单）。
2. **安全体检**：还没请人把代码里跟安全相关的部分查一遍（对外发布前负责任的做法）。
3. **仓库还设成私密**：现在只有自己人能看到代码，公开前要按清单逐项确认（项目里已有一份待办清单）。

A 队就是把这三件事办完，然后正式发布。因为第 1 件涉及花钱、第 3 件涉及"让全世界看到"，都需要你在场拍板，所以放最后，正好留时间给 C、B 攒料。

## 每队"怎么算做完"

- **C 队做完** = 开了中文开关后，常用命令的帮助、表格、报错全是中文；关了开关还是英文。
- **B 队每项做完** = 对应命令能用、有测试兜底、说明书更新。
- **A 队做完** = 新版在 GitHub 上正式发布，任何人能下载安装。

## 你的决定（留档）

1. **顺序**：C（中文）→ B（功能）→ A（发布）。
2. **中文范围**：①帮助文字 ②表格/报告 ③全部报错信息——一步到位全做；④程序来源说明文字暂缓。
3. **批准后的第一步**：先写三份人话版文档放进项目（就是本文和另外两份），代码一行不碰，你看完再动工。
4. **功能取舍（2026-08 追加）**：手机推送（notify）发布前删除；up 保留（你平时不用 topgrade，它就是你的"一键升级"）；support 保留；receipt 并入 doctor。

## 风险提醒（人话）

- C 队：中文比英文"宽"，表格列可能对不齐，要修排版（方案里写了）。
- B 队：B2、B3 要用到 GitHub 的公开查询服务，有次数限制，要省着用。
- A 队：需要花钱和人工决策，卡点不在代码。

## 极限测试记录（2026-08，沙盒内完成，未碰真实系统）

对 hukou 做了一轮"找茬"测试：17 种怪文件（中文/空格/emoji/制表符/200 字符长名/横杠开头/坏链接/自环链接/假 zip/无执行权/空文件/隐藏文件）、空 PATH、死 PATH、保留名收编、重复收编、并发抢锁、升级中途失败、2000 个文件大扫荡。结果：

- **过关**：怪文件名全部正常处理且不乱输出；空/死 PATH 不崩溃；重复收编、并发抢锁、目录/断链收编都被明确拒绝；升级失败后账本和文件完好（补偿机制生效）；3000 个文件 0.21 秒扫完。
- **发现 2 个真问题**（已列入 A 队减负第 4、5 项）：①保留名收编被拒后留下半截垃圾、账本变"体检不过"；②收编起的名字和安装包内程序名不一致时，以后永远升不了级。
- **小瑕疵（暂不处理）**：文本模式盘点不解释"为什么跳过某文件"（JSON 模式有解释）；超长文件名会把表格撑宽。
